# 파이썬 자주 사용하는 라이브러리 추천 목록

## 1. 웹 개발 (Web Development)

### 1.1 Flask
**용도**: 경량 웹 프레임워크

```python
from flask import Flask

app = Flask(__name__)

@app.route('/')
def hello():
    return 'Hello, World!'

if __name__ == '__main__':
    app.run()
```

**설치**: `pip install flask`

**특징**: 
- 가볍고 유연함
- 마이크로서비스에 적합
- 확장 가능한 구조

### 1.2 Django
**용도**: 풀스택 웹 프레임워크

```python
# settings.py, urls.py, views.py 등으로 구성
# 관리자 패널, ORM, 인증 등 많은 기능 내장
```

**설치**: `pip install django`

**특징**:
- 대규모 프로젝트에 적합
- 많은 기능 내장 (ORM, 관리자, 인증 등)
- "Batteries included" 철학

### 1.3 FastAPI
**용도**: 고성능 API 프레임워크

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def read_root():
    return {"Hello": "World"}

@app.get("/items/{item_id}")
def read_item(item_id: int):
    return {"item_id": item_id}
```

**설치**: `pip install fastapi uvicorn`

**특징**:
- 자동 API 문서 생성
- 타입 힌팅 기반
- 비동기 지원
- 매우 빠른 성능

### 1.4 Requests
**용도**: HTTP 라이브러리

```python
import requests

response = requests.get('https://api.github.com')
data = response.json()
print(data)

# POST 요청
response = requests.post('https://httpbin.org/post', 
                        json={'key': 'value'})
```

**설치**: `pip install requests`

**특징**:
- 간단하고 직관적인 API
- 세션, 쿠키, 인증 지원
- 가장 널리 사용되는 HTTP 라이브러리

### 1.5 BeautifulSoup4
**용도**: HTML/XML 파싱

```python
from bs4 import BeautifulSoup
import requests

html = requests.get('https://example.com').text
soup = BeautifulSoup(html, 'html.parser')

title = soup.find('title').text
links = [a['href'] for a in soup.find_all('a')]
```

**설치**: `pip install beautifulsoup4`

**특징**:
- 웹 스크래핑에 필수
- 간단한 파싱 API
- lxml과 함께 사용 시 빠름

## 2. 데이터 처리 (Data Processing)

### 2.1 NumPy
**용도**: 수치 계산 라이브러리

```python
import numpy as np

# 배열 생성
arr = np.array([1, 2, 3, 4, 5])
matrix = np.array([[1, 2], [3, 4]])

# 연산
result = arr * 2
sum_result = np.sum(arr)
mean_result = np.mean(arr)
```

**설치**: `pip install numpy`

**특징**:
- 다차원 배열 처리
- 빠른 수치 연산
- 과학 계산의 기초

### 2.2 Pandas
**용도**: 데이터 분석 라이브러리

```python
import pandas as pd

# 데이터프레임 생성
df = pd.DataFrame({
    'name': ['홍길동', '김철수', '이영희'],
    'age': [25, 30, 28],
    'city': ['서울', '부산', '대구']
})

# 데이터 조작
df_filtered = df[df['age'] > 25]
df_grouped = df.groupby('city').mean()
df.to_csv('output.csv')
```

**설치**: `pip install pandas`

**특징**:
- 테이블 데이터 처리
- CSV, Excel 읽기/쓰기
- 데이터 분석에 필수

### 2.3 Matplotlib
**용도**: 데이터 시각화

```python
import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(0, 10, 100)
y = np.sin(x)

plt.plot(x, y)
plt.xlabel('X axis')
plt.ylabel('Y axis')
plt.title('Sine Wave')
plt.show()
```

**설치**: `pip install matplotlib`

**특징**:
- 다양한 그래프 타입
- 커스터마이징 가능
- 가장 널리 사용되는 시각화 라이브러리

### 2.4 Seaborn
**용도**: 통계 데이터 시각화

```python
import seaborn as sns
import pandas as pd

df = pd.read_csv('data.csv')
sns.scatterplot(data=df, x='x', y='y', hue='category')
sns.heatmap(df.corr(), annot=True)
```

**설치**: `pip install seaborn`

**특징**:
- 통계 그래프에 특화
- Matplotlib 기반
- 아름다운 기본 스타일

## 3. 데이터베이스 (Database)

### 3.1 SQLAlchemy
**용도**: ORM (Object-Relational Mapping)

```python
from sqlalchemy import create_engine, Column, Integer, String
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker

Base = declarative_base()

class User(Base):
    __tablename__ = 'users'
    id = Column(Integer, primary_key=True)
    name = Column(String)

engine = create_engine('sqlite:///example.db')
Base.metadata.create_all(engine)

Session = sessionmaker(bind=engine)
session = Session()

user = User(name='홍길동')
session.add(user)
session.commit()
```

**설치**: `pip install sqlalchemy`

**특징**:
- 다양한 데이터베이스 지원
- ORM과 SQL 모두 지원
- Flask/Django와 통합 가능

### 3.2 psycopg2
**용도**: PostgreSQL 어댑터

```python
import psycopg2

conn = psycopg2.connect(
    host="localhost",
    database="mydb",
    user="user",
    password="password"
)
cur = conn.cursor()
cur.execute("SELECT * FROM users")
rows = cur.fetchall()
```

**설치**: `pip install psycopg2-binary`

**특징**:
- PostgreSQL 공식 어댑터
- 빠른 성능
- 풍부한 기능

### 3.3 pymongo
**용도**: MongoDB 드라이버

```python
from pymongo import MongoClient

client = MongoClient('mongodb://localhost:27017/')
db = client['mydatabase']
collection = db['users']

# 삽입
collection.insert_one({'name': '홍길동', 'age': 25})

# 조회
user = collection.find_one({'name': '홍길동'})
```

**설치**: `pip install pymongo`

**특징**:
- MongoDB 공식 드라이버
- 간단한 API
- NoSQL 데이터베이스

## 4. 비동기 프로그래밍 (Async Programming)

### 4.1 asyncio
**용도**: 비동기 I/O (내장 라이브러리)

```python
import asyncio

async def fetch_data():
    await asyncio.sleep(1)
    return "Data"

async def main():
    result = await fetch_data()
    print(result)

asyncio.run(main())
```

**특징**:
- Python 3.4+ 내장
- 비동기 프로그래밍의 기초
- 코루틴 지원

### 4.2 aiohttp
**용도**: 비동기 HTTP 클라이언트/서버

```python
import aiohttp
import asyncio

async def fetch(session, url):
    async with session.get(url) as response:
        return await response.text()

async def main():
    async with aiohttp.ClientSession() as session:
        html = await fetch(session, 'https://example.com')
        print(html)

asyncio.run(main())
```

**설치**: `pip install aiohttp`

**특징**:
- 비동기 HTTP 요청
- FastAPI와 함께 사용
- 높은 성능

## 5. 테스트 (Testing)

### 5.1 pytest
**용도**: 테스트 프레임워크

```python
# test_example.py
def test_add():
    assert 1 + 1 == 2

def test_multiply():
    assert 2 * 3 == 6

# 실행: pytest test_example.py
```

**설치**: `pip install pytest`

**특징**:
- 간단한 문법
- 풍부한 플러그인
- 가장 인기 있는 테스트 프레임워크

### 5.2 unittest
**용도**: 테스트 프레임워크 (내장)

```python
import unittest

class TestMath(unittest.TestCase):
    def test_add(self):
        self.assertEqual(1 + 1, 2)

if __name__ == '__main__':
    unittest.main()
```

**특징**:
- Python 내장
- JUnit 스타일
- 표준 라이브러리

### 5.3 coverage
**용도**: 코드 커버리지 측정

```python
# pytest와 함께 사용
# pytest --cov=my_package tests/
```

**설치**: `pip install coverage`

**특징**:
- 테스트 커버리지 측정
- 리포트 생성
- 품질 관리에 필수

## 6. 환경 변수 및 설정 (Configuration)

### 6.1 python-dotenv
**용도**: 환경 변수 관리

```python
from dotenv import load_dotenv
import os

load_dotenv()

database_url = os.getenv('DATABASE_URL')
secret_key = os.getenv('SECRET_KEY')
```

**설치**: `pip install python-dotenv`

**특징**:
- `.env` 파일 지원
- 설정 관리 간소화
- 보안 정보 관리

### 6.2 pydantic
**용도**: 데이터 검증 및 설정 관리

```python
from pydantic import BaseModel

class User(BaseModel):
    name: str
    age: int
    email: str

user = User(name="홍길동", age=25, email="hong@example.com")
print(user.name)
```

**설치**: `pip install pydantic`

**특징**:
- 타입 검증
- FastAPI와 통합
- 설정 관리에 유용

## 7. 날짜 및 시간 (Date & Time)

### 7.1 python-dateutil
**용도**: 날짜/시간 처리 확장

```python
from dateutil import parser, relativedelta
from datetime import datetime

# 문자열을 날짜로 변환
date = parser.parse("2024-01-15")

# 날짜 계산
next_month = date + relativedelta.relativedelta(months=1)
```

**설치**: `pip install python-dateutil`

**특징**:
- 유연한 날짜 파싱
- 상대 날짜 계산
- 표준 라이브러리 확장

### 7.2 pytz
**용도**: 시간대 처리

```python
from datetime import datetime
import pytz

# 시간대 설정
seoul = pytz.timezone('Asia/Seoul')
dt = datetime.now(seoul)
print(dt)
```

**설치**: `pip install pytz`

**특징**:
- 시간대 변환
- IANA 시간대 데이터베이스
- 국제화에 필수

## 8. 파일 처리 (File Processing)

### 8.1 openpyxl
**용도**: Excel 파일 처리

```python
from openpyxl import Workbook, load_workbook

# 새 워크북 생성
wb = Workbook()
ws = wb.active
ws['A1'] = 'Hello'
wb.save('example.xlsx')

# 기존 파일 읽기
wb = load_workbook('example.xlsx')
ws = wb.active
print(ws['A1'].value)
```

**설치**: `pip install openpyxl`

**특징**:
- Excel 읽기/쓰기
- .xlsx 형식 지원
- 셀 스타일링 가능

### 8.2 PyPDF2 / pypdf
**용도**: PDF 파일 처리

```python
import PyPDF2

with open('document.pdf', 'rb') as file:
    reader = PyPDF2.PdfReader(file)
    num_pages = len(reader.pages)
    page = reader.pages[0]
    text = page.extract_text()
```

**설치**: `pip install PyPDF2` 또는 `pip install pypdf`

**특징**:
- PDF 읽기/쓰기
- 텍스트 추출
- 페이지 조작

## 9. 이미지 처리 (Image Processing)

### 9.1 Pillow (PIL)
**용도**: 이미지 처리

```python
from PIL import Image

# 이미지 열기
img = Image.open('photo.jpg')

# 크기 조정
img_resized = img.resize((800, 600))

# 저장
img_resized.save('resized_photo.jpg')

# 필터 적용
from PIL import ImageFilter
img_filtered = img.filter(ImageFilter.BLUR)
```

**설치**: `pip install Pillow`

**특징**:
- 다양한 이미지 형식 지원
- 이미지 조작 기능
- 가장 널리 사용되는 이미지 라이브러리

### 9.2 OpenCV
**용도**: 컴퓨터 비전

```python
import cv2

# 이미지 읽기
img = cv2.imread('photo.jpg')

# 그레이스케일 변환
gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)

# 이미지 저장
cv2.imwrite('gray.jpg', gray)

# 얼굴 인식 등 고급 기능
```

**설치**: `pip install opencv-python`

**특징**:
- 컴퓨터 비전 알고리즘
- 실시간 이미지 처리
- 머신러닝 통합

## 10. 머신러닝 (Machine Learning)

### 10.1 scikit-learn
**용도**: 머신러닝 라이브러리

```python
from sklearn import datasets
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier

# 데이터 로드
iris = datasets.load_iris()
X, y = iris.data, iris.target

# 데이터 분할
X_train, X_test, y_train, y_test = train_test_split(X, y)

# 모델 학습
model = RandomForestClassifier()
model.fit(X_train, y_train)

# 예측
predictions = model.predict(X_test)
```

**설치**: `pip install scikit-learn`

**특징**:
- 다양한 머신러닝 알고리즘
- 간단한 API
- 데이터 전처리 도구

### 10.2 TensorFlow
**용도**: 딥러닝 프레임워크

```python
import tensorflow as tf

# 간단한 모델 생성
model = tf.keras.Sequential([
    tf.keras.layers.Dense(128, activation='relu'),
    tf.keras.layers.Dense(10, activation='softmax')
])

model.compile(optimizer='adam', loss='sparse_categorical_crossentropy')
model.fit(x_train, y_train, epochs=5)
```

**설치**: `pip install tensorflow`

**특징**:
- 구글 개발
- 대규모 딥러닝 모델
- 프로덕션 배포 지원

### 10.3 PyTorch
**용도**: 딥러닝 프레임워크

```python
import torch
import torch.nn as nn

# 간단한 신경망
class Net(nn.Module):
    def __init__(self):
        super().__init__()
        self.fc1 = nn.Linear(784, 128)
        self.fc2 = nn.Linear(128, 10)

    def forward(self, x):
        x = torch.relu(self.fc1(x))
        x = self.fc2(x)
        return x

model = Net()
```

**설치**: `pip install torch`

**특징**:
- Facebook 개발
- 연구에 널리 사용
- 동적 계산 그래프

## 11. 유틸리티 (Utilities)

### 11.1 tqdm
**용도**: 진행률 표시

```python
from tqdm import tqdm
import time

# 반복문 진행률 표시
for i in tqdm(range(100)):
    time.sleep(0.1)
```

**설치**: `pip install tqdm`

**특징**:
- 진행률 바 표시
- 간단한 사용법
- 다양한 환경 지원

### 11.2 click
**용도**: CLI 도구 생성

```python
import click

@click.command()
@click.option('--name', default='World', help='이름')
@click.option('--count', default=1, help='반복 횟수')
def hello(name, count):
    for _ in range(count):
        click.echo(f'Hello {name}!')

if __name__ == '__main__':
    hello()
```

**설치**: `pip install click`

**특징**:
- 간단한 CLI 생성
- 자동 도움말 생성
- Flask와 통합

### 11.3 python-dotenv
**용도**: 환경 변수 관리 (이미 언급됨)

### 11.4 python-decouple
**용도**: 설정 관리

```python
from decouple import config

database_url = config('DATABASE_URL')
debug = config('DEBUG', default=False, cast=bool)
```

**설치**: `pip install python-decouple`

**특징**:
- 설정 분리
- 타입 캐스팅
- Django에 적합

## 12. 로깅 (Logging)

### 12.1 loguru
**용도**: 고급 로깅 라이브러리

```python
from loguru import logger

logger.info("정보 메시지")
logger.warning("경고 메시지")
logger.error("오류 메시지")

# 파일 로깅
logger.add("file_{time}.log", rotation="500 MB")
```

**설치**: `pip install loguru`

**특징**:
- 간단한 API
- 자동 로테이션
- 색상 출력

## 13. 인증 및 보안 (Authentication & Security)

### 13.1 python-jose
**용도**: JWT 토큰 처리

```python
from jose import jwt

# 토큰 생성
token = jwt.encode({'user_id': 123}, 'secret', algorithm='HS256')

# 토큰 검증
payload = jwt.decode(token, 'secret', algorithms=['HS256'])
```

**설치**: `pip install python-jose[cryptography]`

**특징**:
- JWT 토큰 생성/검증
- FastAPI와 통합
- 보안 인증

### 13.2 passlib
**용도**: 비밀번호 해싱

```python
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

# 비밀번호 해싱
hashed = pwd_context.hash("password123")

# 비밀번호 검증
is_valid = pwd_context.verify("password123", hashed)
```

**설치**: `pip install passlib[bcrypt]`

**특징**:
- 다양한 해싱 알고리즘
- 비밀번호 검증
- 보안 강화

## 14. 작업 스케줄링 (Task Scheduling)

### 14.1 celery
**용도**: 분산 작업 큐

```python
from celery import Celery

app = Celery('tasks', broker='redis://localhost:6379')

@app.task
def add(x, y):
    return x + y

# 비동기 실행
result = add.delay(4, 4)
```

**설치**: `pip install celery`

**특징**:
- 비동기 작업 처리
- 분산 처리
- Django와 통합

### 14.2 APScheduler
**용도**: 작업 스케줄링

```python
from apscheduler.schedulers.blocking import BlockingScheduler

def job():
    print("작업 실행!")

scheduler = BlockingScheduler()
scheduler.add_job(job, 'interval', seconds=10)
scheduler.start()
```

**설치**: `pip install apscheduler`

**특징**:
- 스케줄 작업 실행
- 다양한 트리거
- 간단한 사용법

## 15. 데이터 검증 (Data Validation)

### 15.1 marshmallow
**용도**: 객체 직렬화 및 검증

```python
from marshmallow import Schema, fields

class UserSchema(Schema):
    name = fields.Str(required=True)
    age = fields.Int(required=True)
    email = fields.Email()

# 검증
schema = UserSchema()
result = schema.load({'name': '홍길동', 'age': 25, 'email': 'hong@example.com'})
```

**설치**: `pip install marshmallow`

**특징**:
- 데이터 검증
- 직렬화/역직렬화
- Flask와 통합

## 16. API 문서화 (API Documentation)

### 16.1 Swagger / OpenAPI
**용도**: API 문서 자동 생성

```python
# FastAPI는 자동으로 Swagger 문서 생성
# /docs 엔드포인트에서 확인 가능
```

**특징**:
- API 문서 자동 생성
- 인터랙티브 문서
- 표준화된 형식

## 17. 카테고리별 추천 라이브러리 요약

### 웹 개발
- **Flask**: 경량 웹 프레임워크
- **Django**: 풀스택 웹 프레임워크
- **FastAPI**: 고성능 API
- **Requests**: HTTP 클라이언트

### 데이터 처리
- **NumPy**: 수치 계산
- **Pandas**: 데이터 분석
- **Matplotlib**: 데이터 시각화
- **Seaborn**: 통계 시각화

### 데이터베이스
- **SQLAlchemy**: ORM
- **psycopg2**: PostgreSQL
- **pymongo**: MongoDB

### 테스트
- **pytest**: 테스트 프레임워크
- **coverage**: 커버리지 측정

### 머신러닝
- **scikit-learn**: 전통적 ML
- **TensorFlow**: 딥러닝
- **PyTorch**: 딥러닝

### 유틸리티
- **python-dotenv**: 환경 변수
- **tqdm**: 진행률 표시
- **click**: CLI 도구

## 18. 설치 및 관리

### 18.1 requirements.txt 예제

```txt
# 웹 개발
Flask==2.3.0
FastAPI==0.100.0
requests==2.31.0

# 데이터 처리
pandas==2.0.0
numpy==1.24.0
matplotlib==3.7.0

# 데이터베이스
SQLAlchemy==2.0.0
psycopg2-binary==2.9.0

# 테스트
pytest==7.4.0
coverage==7.2.0

# 유틸리티
python-dotenv==1.0.0
tqdm==4.65.0
click==8.1.0
```

### 18.2 가상 환경 사용

```bash
# 가상 환경 생성
python -m venv venv

# 활성화 (Windows)
venv\Scripts\activate

# 활성화 (Linux/Mac)
source venv/bin/activate

# 패키지 설치
pip install -r requirements.txt
```

## 19. 라이브러리 선택 가이드

### 프로젝트 타입별 추천

| 프로젝트 타입 | 추천 라이브러리 |
|------------|--------------|
| **웹 API** | FastAPI, Requests |
| **웹 애플리케이션** | Django, Flask |
| **데이터 분석** | Pandas, NumPy, Matplotlib |
| **머신러닝** | scikit-learn, TensorFlow, PyTorch |
| **웹 스크래핑** | Requests, BeautifulSoup4 |
| **CLI 도구** | Click, argparse (내장) |
| **비동기 작업** | asyncio, aiohttp, celery |

## 20. 요약

### 필수 라이브러리 Top 10

1. **Requests** - HTTP 요청
2. **Pandas** - 데이터 분석
3. **NumPy** - 수치 계산
4. **Matplotlib** - 데이터 시각화
5. **pytest** - 테스트
6. **python-dotenv** - 환경 변수
7. **SQLAlchemy** - 데이터베이스 ORM
8. **Flask/FastAPI** - 웹 프레임워크
9. **BeautifulSoup4** - 웹 스크래핑
10. **tqdm** - 진행률 표시

### 선택 기준

- ✅ 프로젝트 요구사항에 맞는지
- ✅ 활발한 커뮤니티와 문서
- ✅ 최근 업데이트 여부
- ✅ 라이선스 호환성
- ✅ 성능과 기능의 균형

