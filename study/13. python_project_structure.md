# 파이썬 프로젝트 구조 가이드

## 1. 순수 파이썬 프로젝트 (Pure Python)

### 1.1 기본 구조 (간단한 스크립트)

```
project_name/
├── main.py              # 메인 실행 파일
├── utils.py             # 유틸리티 함수
├── config.py            # 설정 파일
└── requirements.txt     # 의존성 목록
```

### 1.2 표준 패키지 구조 (권장)

```
project_name/
├── project_name/        # 메인 패키지 디렉토리
│   ├── __init__.py     # 패키지 초기화
│   ├── main.py         # 메인 모듈
│   ├── models.py       # 데이터 모델
│   ├── utils.py        # 유틸리티 함수
│   ├── config.py       # 설정
│   └── exceptions.py   # 커스텀 예외
├── tests/              # 테스트 코드
│   ├── __init__.py
│   ├── test_main.py
│   └── test_utils.py
├── docs/               # 문서
│   └── README.md
├── scripts/            # 실행 스크립트
│   └── run.py
├── requirements.txt    # 의존성 목록
├── setup.py           # 패키지 설치 설정 (선택)
├── .gitignore         # Git 제외 파일
└── README.md          # 프로젝트 설명
```

### 1.3 고급 구조 (대규모 프로젝트)

```
project_name/
├── project_name/
│   ├── __init__.py
│   ├── core/           # 핵심 기능
│   │   ├── __init__.py
│   │   ├── base.py
│   │   └── exceptions.py
│   ├── models/         # 데이터 모델
│   │   ├── __init__.py
│   │   ├── user.py
│   │   └── product.py
│   ├── services/       # 비즈니스 로직
│   │   ├── __init__.py
│   │   ├── user_service.py
│   │   └── product_service.py
│   ├── utils/          # 유틸리티
│   │   ├── __init__.py
│   │   ├── helpers.py
│   │   └── validators.py
│   └── config/         # 설정
│       ├── __init__.py
│       ├── settings.py
│       └── constants.py
├── tests/
│   ├── unit/           # 단위 테스트
│   ├── integration/   # 통합 테스트
│   └── fixtures/       # 테스트 데이터
├── docs/
├── scripts/
├── requirements/
│   ├── base.txt        # 기본 의존성
│   ├── dev.txt         # 개발 의존성
│   └── prod.txt        # 프로덕션 의존성
├── .env.example        # 환경 변수 예제
├── .gitignore
├── setup.py
└── README.md
```

### 1.4 예제 파일

#### `project_name/__init__.py`
```python
"""
Project Name - 프로젝트 설명
"""

__version__ = "0.1.0"
__author__ = "Your Name"

from .main import main_function
from .models import BaseModel

__all__ = ['main_function', 'BaseModel']
```

#### `setup.py` (패키지 배포용)
```python
from setuptools import setup, find_packages

setup(
    name="project_name",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # requirements.txt의 내용
    ],
    python_requires=">=3.8",
)
```

#### `requirements.txt`
```
# 기본 의존성
requests>=2.28.0
python-dotenv>=0.19.0

# 개발 의존성 (requirements-dev.txt로 분리 가능)
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0
```

## 2. Flask 프로젝트 구조

### 2.1 기본 구조 (소규모 프로젝트)

```
flask_app/
├── app.py              # Flask 애플리케이션
├── templates/          # HTML 템플릿
│   ├── base.html
│   └── index.html
├── static/             # 정적 파일
│   ├── css/
│   ├── js/
│   └── images/
├── requirements.txt
└── README.md
```

### 2.2 표준 구조 (권장 - Application Factory 패턴)

```
flask_app/
├── app/
│   ├── __init__.py     # 애플리케이션 팩토리
│   ├── models.py      # 데이터베이스 모델
│   ├── routes.py      # 라우트 정의
│   ├── forms.py       # WTForms (선택)
│   ├── config.py      # 설정 클래스
│   ├── extensions.py  # 확장 초기화 (DB, Migrate 등)
│   ├── templates/     # 템플릿
│   │   ├── base.html
│   │   ├── index.html
│   │   └── errors/
│   │       ├── 404.html
│   │       └── 500.html
│   ├── static/        # 정적 파일
│   │   ├── css/
│   │   ├── js/
│   │   └── images/
│   └── blueprints/    # 블루프린트 (선택)
│       ├── __init__.py
│       ├── auth/
│       │   ├── __init__.py
│       │   ├── routes.py
│       │   └── forms.py
│       └── main/
│           ├── __init__.py
│           └── routes.py
├── migrations/         # Flask-Migrate 마이그레이션
├── tests/
│   ├── __init__.py
│   ├── test_routes.py
│   └── test_models.py
├── instance/          # 인스턴스별 설정 (gitignore)
│   └── config.py
├── .env               # 환경 변수 (gitignore)
├── .flaskenv          # Flask 환경 변수
├── requirements.txt
├── .gitignore
└── README.md
```

### 2.3 고급 구조 (대규모 프로젝트)

```
flask_app/
├── app/
│   ├── __init__.py
│   ├── config.py
│   ├── extensions.py
│   ├── models/         # 모델 분리
│   │   ├── __init__.py
│   │   ├── user.py
│   │   └── post.py
│   ├── api/            # REST API
│   │   ├── __init__.py
│   │   ├── routes.py
│   │   └── schemas.py  # Marshmallow 등
│   ├── web/            # 웹 라우트
│   │   ├── __init__.py
│   │   └── routes.py
│   ├── services/       # 비즈니스 로직
│   │   ├── __init__.py
│   │   ├── user_service.py
│   │   └── email_service.py
│   ├── utils/          # 유틸리티
│   │   ├── __init__.py
│   │   ├── helpers.py
│   │   └── validators.py
│   ├── templates/
│   ├── static/
│   └── blueprints/
│       ├── auth/
│       ├── main/
│       └── admin/
├── migrations/
├── tests/
│   ├── conftest.py     # pytest 설정
│   ├── unit/
│   ├── integration/
│   └── fixtures/
├── instance/
├── scripts/            # 관리 스크립트
│   └── init_db.py
├── .env
├── .flaskenv
├── requirements/
│   ├── base.txt
│   ├── dev.txt
│   └── prod.txt
├── .gitignore
└── README.md
```

### 2.4 예제 파일

#### `app/__init__.py` (Application Factory)
```python
from flask import Flask
from app.config import Config
from app.extensions import db, migrate

def create_app(config_class=Config):
    app = Flask(__name__)
    app.config.from_object(config_class)
    
    # 확장 초기화
    db.init_app(app)
    migrate.init_app(app, db)
    
    # 블루프린트 등록
    from app.blueprints.main import bp as main_bp
    from app.blueprints.auth import bp as auth_bp
    app.register_blueprint(main_bp)
    app.register_blueprint(auth_bp, url_prefix='/auth')
    
    return app
```

#### `app/config.py`
```python
import os
from dotenv import load_dotenv

load_dotenv()

class Config:
    SECRET_KEY = os.environ.get('SECRET_KEY') or 'dev-secret-key'
    SQLALCHEMY_DATABASE_URI = os.environ.get('DATABASE_URL') or \
        'sqlite:///app.db'
    SQLALCHEMY_TRACK_MODIFICATIONS = False

class DevelopmentConfig(Config):
    DEBUG = True

class ProductionConfig(Config):
    DEBUG = False
```

#### `app/extensions.py`
```python
from flask_sqlalchemy import SQLAlchemy
from flask_migrate import Migrate

db = SQLAlchemy()
migrate = Migrate()
```

#### `run.py` (애플리케이션 실행)
```python
from app import create_app

app = create_app()

if __name__ == '__main__':
    app.run(debug=True)
```

#### `requirements.txt` (Flask)
```
Flask>=2.3.0
Flask-SQLAlchemy>=3.0.0
Flask-Migrate>=4.0.0
Flask-Login>=0.6.0
Flask-WTF>=1.1.0
WTForms>=3.0.0
python-dotenv>=0.19.0
```

## 3. Django 프로젝트 구조

### 3.1 기본 구조 (Django 프로젝트)

```
django_project/
├── manage.py           # Django 관리 스크립트
├── django_project/     # 프로젝트 설정 디렉토리
│   ├── __init__.py
│   ├── settings.py     # 설정
│   ├── urls.py         # 메인 URL 설정
│   ├── wsgi.py         # WSGI 설정
│   └── asgi.py         # ASGI 설정 (Django 3.0+)
├── app1/               # 앱 1
│   ├── __init__.py
│   ├── admin.py        # 관리자 설정
│   ├── apps.py         # 앱 설정
│   ├── models.py       # 모델
│   ├── views.py        # 뷰
│   ├── urls.py         # 앱 URL
│   ├── forms.py        # 폼
│   ├── serializers.py # DRF 시리얼라이저 (선택)
│   ├── migrations/     # 마이그레이션
│   ├── templates/      # 템플릿
│   │   └── app1/
│   ├── static/         # 정적 파일
│   │   └── app1/
│   └── tests.py        # 테스트
├── app2/               # 앱 2
├── static/             # 프로젝트 레벨 정적 파일
├── media/              # 업로드된 파일 (gitignore)
├── templates/          # 프로젝트 레벨 템플릿
│   └── base.html
├── requirements.txt
├── .env                # 환경 변수 (gitignore)
├── .gitignore
└── README.md
```

### 3.2 표준 구조 (권장)

```
django_project/
├── manage.py
├── config/             # 프로젝트 설정 디렉토리 (이름 변경)
│   ├── __init__.py
│   ├── settings/
│   │   ├── __init__.py
│   │   ├── base.py     # 공통 설정
│   │   ├── development.py
│   │   ├── production.py
│   │   └── testing.py
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
├── apps/               # 앱들을 그룹화
│   ├── __init__.py
│   ├── accounts/       # 사용자 관리 앱
│   │   ├── __init__.py
│   │   ├── admin.py
│   │   ├── apps.py
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── urls.py
│   │   ├── forms.py
│   │   ├── serializers.py
│   │   ├── migrations/
│   │   ├── templates/
│   │   ├── static/
│   │   └── tests/
│   ├── blog/           # 블로그 앱
│   └── api/            # API 앱 (DRF)
│       ├── __init__.py
│       ├── views.py
│       ├── urls.py
│       ├── serializers.py
│       └── permissions.py
├── common/             # 공통 유틸리티
│   ├── __init__.py
│   ├── utils.py
│   ├── mixins.py
│   └── decorators.py
├── static/             # 프로젝트 레벨 정적 파일
├── media/              # 미디어 파일
├── templates/          # 프로젝트 레벨 템플릿
│   ├── base.html
│   ├── includes/
│   └── errors/
├── locale/             # 번역 파일 (선택)
├── requirements/
│   ├── base.txt
│   ├── development.txt
│   └── production.txt
├── .env
├── .gitignore
└── README.md
```

### 3.3 예제 파일

#### `config/settings/base.py`
```python
import os
from pathlib import Path
from dotenv import load_dotenv

load_dotenv()

BASE_DIR = Path(__file__).resolve().parent.parent.parent

SECRET_KEY = os.environ.get('SECRET_KEY', 'dev-secret-key')

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    # 로컬 앱
    'apps.accounts',
    'apps.blog',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'config.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'templates'],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

STATIC_URL = '/static/'
STATICFILES_DIRS = [BASE_DIR / 'static']
STATIC_ROOT = BASE_DIR / 'staticfiles'

MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'
```

#### `config/settings/development.py`
```python
from .base import *

DEBUG = True

ALLOWED_HOSTS = ['localhost', '127.0.0.1']

# 개발용 추가 설정
INSTALLED_APPS += [
    'debug_toolbar',
]

MIDDLEWARE += [
    'debug_toolbar.middleware.DebugToolbarMiddleware',
]
```

#### `requirements.txt` (Django)
```
Django>=4.2.0
django-environ>=0.10.0
Pillow>=10.0.0
djangorestframework>=3.14.0  # DRF 사용 시
django-cors-headers>=4.0.0    # CORS 설정 시
```

## 4. FastAPI 프로젝트 구조

### 4.1 기본 구조

```
fastapi_app/
├── app/
│   ├── __init__.py
│   ├── main.py         # FastAPI 애플리케이션
│   ├── config.py       # 설정
│   ├── database.py     # 데이터베이스 연결
│   ├── models/         # SQLAlchemy 모델
│   │   ├── __init__.py
│   │   └── user.py
│   ├── schemas/        # Pydantic 스키마
│   │   ├── __init__.py
│   │   └── user.py
│   ├── api/            # API 라우터
│   │   ├── __init__.py
│   │   ├── deps.py     # 의존성
│   │   └── v1/
│   │       ├── __init__.py
│   │       ├── users.py
│   │       └── items.py
│   ├── crud/           # CRUD 작업
│   │   ├── __init__.py
│   │   └── user.py
│   └── utils/          # 유틸리티
│       ├── __init__.py
│       └── security.py
├── tests/
│   ├── __init__.py
│   ├── conftest.py
│   └── test_api/
├── alembic/            # 마이그레이션 (선택)
│   └── versions/
├── .env
├── requirements.txt
├── .gitignore
└── README.md
```

### 4.2 예제 파일

#### `app/main.py`
```python
from fastapi import FastAPI
from app.api.v1 import users, items
from app.config import settings

app = FastAPI(
    title="My API",
    version="1.0.0",
    description="FastAPI application"
)

app.include_router(users.router, prefix="/api/v1/users", tags=["users"])
app.include_router(items.router, prefix="/api/v1/items", tags=["items"])

@app.get("/")
async def root():
    return {"message": "Hello World"}
```

#### `app/api/v1/users.py`
```python
from fastapi import APIRouter, Depends
from app.schemas.user import UserCreate, UserResponse
from app.crud import user as crud_user

router = APIRouter()

@router.post("/", response_model=UserResponse)
async def create_user(user: UserCreate):
    return crud_user.create_user(user)

@router.get("/{user_id}", response_model=UserResponse)
async def get_user(user_id: int):
    return crud_user.get_user(user_id)
```

#### `requirements.txt` (FastAPI)
```
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
alembic>=1.11.0
```

## 5. 패키지 배포 구조 (PyPI 배포용)

### 5.1 표준 구조

```
package_name/
├── package_name/       # 소스 코드
│   ├── __init__.py
│   ├── module1.py
│   └── module2.py
├── tests/
│   ├── __init__.py
│   └── test_module1.py
├── docs/
├── examples/
├── setup.py           # 또는 pyproject.toml
├── setup.cfg
├── MANIFEST.in        # 포함할 추가 파일
├── LICENSE
├── README.md
├── CHANGELOG.md
├── .gitignore
└── requirements.txt
```

### 5.2 `setup.py` 예제

```python
from setuptools import setup, find_packages

with open("README.md", "r", encoding="utf-8") as fh:
    long_description = fh.read()

setup(
    name="package_name",
    version="0.1.0",
    author="Your Name",
    author_email="your.email@example.com",
    description="Short description",
    long_description=long_description,
    long_description_content_type="text/markdown",
    url="https://github.com/username/package_name",
    packages=find_packages(),
    classifiers=[
        "Programming Language :: Python :: 3",
        "License :: OSI Approved :: MIT License",
        "Operating System :: OS Independent",
    ],
    python_requires=">=3.8",
    install_requires=[
        "requests>=2.28.0",
    ],
    extras_require={
        "dev": [
            "pytest>=7.0.0",
            "black>=22.0.0",
        ],
    },
)
```

### 5.3 `pyproject.toml` 예제 (최신 방식)

```toml
[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "package_name"
version = "0.1.0"
description = "Short description"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.28.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=22.0.0",
]
```

## 6. 공통 파일 및 설정

### 6.1 `.gitignore` (Python)

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment variables
.env
.env.local

# Database
*.db
*.sqlite3

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
/media
/staticfiles

# Flask
instance/
.webassets-cache

# Testing
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints

# OS
.DS_Store
Thumbs.db
```

### 6.2 `requirements.txt` 구조화

#### `requirements/base.txt`
```
# 핵심 의존성
requests>=2.28.0
python-dotenv>=0.19.0
```

#### `requirements/dev.txt`
```
-r base.txt

# 개발 도구
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0
```

#### `requirements/prod.txt`
```
-r base.txt

# 프로덕션 전용
gunicorn>=20.1.0
```

### 6.3 환경 변수 관리

#### `.env.example`
```env
# 데이터베이스
DATABASE_URL=postgresql://user:password@localhost/dbname

# 보안
SECRET_KEY=your-secret-key-here

# API 키
API_KEY=your-api-key-here

# 환경
DEBUG=True
ENVIRONMENT=development
```

## 7. 프로젝트 타입별 선택 가이드

### 7.1 프로젝트 타입 선택

| 프로젝트 타입 | 사용 시기 | 특징 |
|------------|---------|------|
| **순수 파이썬** | CLI 도구, 라이브러리, 스크립트 | 간단하고 가벼움 |
| **Flask** | 소규모 웹앱, API, 마이크로서비스 | 유연하고 가벼움 |
| **Django** | 대규모 웹앱, 관리자 패널 필요 | 풀스택, 많은 기능 내장 |
| **FastAPI** | 고성능 API, 비동기 처리 | 빠르고 현대적 |

### 7.2 구조 선택 기준

- **소규모 프로젝트**: 기본 구조 사용
- **중규모 프로젝트**: 표준 구조 사용
- **대규모 프로젝트**: 고급 구조 사용 (모듈 분리, 서비스 레이어)

## 8. 모범 사례

### 8.1 디렉토리 구조 원칙

1. **관심사의 분리**: 기능별로 모듈 분리
2. **재사용성**: 공통 기능은 별도 모듈로
3. **확장성**: 향후 기능 추가를 고려한 구조
4. **명확성**: 디렉토리/파일 이름이 목적을 명확히 표현

### 8.2 파일 명명 규칙

- **모듈**: `snake_case.py` (예: `user_service.py`)
- **클래스**: `PascalCase` (예: `UserService`)
- **함수/변수**: `snake_case` (예: `get_user_data`)
- **상수**: `UPPER_SNAKE_CASE` (예: `MAX_RETRY_COUNT`)

### 8.3 `__init__.py` 활용

```python
# 패키지 레벨에서 주요 클래스/함수 노출
from .models import User, Post
from .services import UserService
from .utils import helper_function

__all__ = ['User', 'Post', 'UserService', 'helper_function']
```

## 9. 요약

### 핵심 포인트

1. **순수 파이썬**: 간단한 패키지 구조, `setup.py` 또는 `pyproject.toml` 사용
2. **Flask**: Application Factory 패턴, 블루프린트 활용
3. **Django**: 앱 기반 구조, 설정 파일 분리
4. **FastAPI**: 라우터 기반, Pydantic 스키마 활용

### 공통 권장사항

- ✅ 환경 변수 사용 (`.env`)
- ✅ `requirements.txt` 분리 (base/dev/prod)
- ✅ 테스트 코드 작성 (`tests/`)
- ✅ `.gitignore` 설정
- ✅ `README.md` 작성
- ✅ 버전 관리 (Git)

### 다음 단계:
1. 프로젝트 타입 선택
2. 기본 구조 생성
3. 의존성 관리 설정
4. 환경 변수 설정
5. 테스트 환경 구성

